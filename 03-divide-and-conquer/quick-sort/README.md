# Quick Sort

Partitions around a pivot and recursively sorts the partitions. Average time is O(n log n), worst case is O(n^2), and stack space depends on partition balance. The three examples show a general partition, Lomuto, and Hoare partitioning.
